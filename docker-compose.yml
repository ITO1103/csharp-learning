services:
  csharp:
    image: mcr.microsoft.com/dotnet/sdk:8.0
    container_name: csharp-learning
    working_dir: /workspace
    volumes:
      - ./workspace:/workspace
    stdin_open: true
    tty: true
